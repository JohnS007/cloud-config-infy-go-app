spring:
  mvc:
    view:
      prefix: /WEB-INF/pages/
      suffix: .jsp

  datasource:
    username: root
    password: johnty
    url: jdbc:mysql://localhost:3306/infy_go_user
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
